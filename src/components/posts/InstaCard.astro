---
interface Props {
    picture: string,
    alt: string,
    link: string,
}

const {picture,alt,link} = Astro.props
---
<div>
    <a href={link}> 
        <img class="rounded-3xl p-4 hover:scale-90 transition-all ease-in-out duration-200" src={picture} alt={alt}>
    </a>
</div>